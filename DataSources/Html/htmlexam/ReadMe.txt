HTML Datasource Example

This is a simple example of using the HTML Data Source.  You are presented
with a dialog to enter the file or URL you wish to import into the grid.  

The datasource uses the WinINET API to connect and retrieve files via the 
internet.  This example works best with files installed along with 
Microsoft (r) Internet Explorer v3.02